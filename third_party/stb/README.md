# stb

This directory vendors `stb_image_write.h` from the upstream `nothings/stb` project for simple PNG artifact output.

The vendored header identifies itself as public domain and is also available under MIT license terms from upstream.
