set(CUBEY_PLANET_SHADERS
    "${CMAKE_SOURCE_DIR}/shaders/cubey/atmosphere/atmosphere.vert"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/atmosphere/atmosphere.frag"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/sky/celestial_body.vert"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/sky/celestial_body.frag"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud.vert"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud_composite_background.frag"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud_composite_background_depth.frag"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud_blue_noise.comp"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud_march.comp"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/surface_cloud_march.comp"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud_perlin_worley.comp"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud_temporal.comp"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud_weather.comp"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud_worley.comp"
    shaders/planet_surface.vert
    shaders/planet_surface.frag
    shaders/planet_local_detail.vert
    "${CMAKE_SOURCE_DIR}/shaders/cubey/forward_pbr/forward_pbr_post.vert"
    "${CMAKE_SOURCE_DIR}/shaders/cubey/forward_pbr/forward_pbr_post.frag"
)

cubey_add_app_target(
    cubey_project_planet
    OUTPUT_NAME planet
    SHADER_DEFINE CUBEY_PLANET_SHADER_DIR
    WINDOWED_SMOKE_TEST planet_reaches_glfw
    WINDOWED_SMOKE_PATTERN "planet: .*rendering planet project"
    SOURCES
        main.cpp
        planet_app.cpp
        planet_atmosphere_adapter.cpp
        planet_camera.cpp
        planet_celestial.cpp
        planet_config.cpp
        planet_frame.cpp
        planet_local_detail.cpp
        planet_local_detail_runtime.cpp
        planet_surface.cpp
        planet_surface_field.cpp
        planet_surface_runtime.cpp
        planet_ui.cpp
    SHADERS
        ${CUBEY_PLANET_SHADERS}
    SHADER_INCLUDE_DIRS
        "${CMAKE_CURRENT_SOURCE_DIR}/shaders"
        "${CMAKE_SOURCE_DIR}/shaders"
    SHADER_DEPENDS
        "${CMAKE_SOURCE_DIR}/shaders/cubey/atmosphere.glsl"
        "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud_common.glsl"
        "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud_noise_common.glsl"
        "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud_resolve_common.glsl"
        "${CMAKE_SOURCE_DIR}/shaders/cubey/cloud/cloud_weather_common.glsl"
        "${CMAKE_CURRENT_SOURCE_DIR}/shaders/planet_atmosphere.glsl"
        "${CMAKE_CURRENT_SOURCE_DIR}/shaders/planet_surface_field.glsl"
        "${CMAKE_SOURCE_DIR}/shaders/cubey/procedural/noise.glsl"
        "${CMAKE_SOURCE_DIR}/shaders/cubey/procedural/random.glsl"
        "${CMAKE_SOURCE_DIR}/shaders/cubey/procedural/operators.glsl"
        "${CMAKE_SOURCE_DIR}/shaders/cubey/color_space.glsl"
        "${CMAKE_SOURCE_DIR}/shaders/cubey/pbr.glsl"
)

if(BUILD_TESTING)
    add_executable(
        cubey_project_planet_tests
        planet_camera.cpp
        planet_config.cpp
        planet_frame.cpp
        planet_surface.cpp
        planet_surface_field.cpp
        planet_frame_tests.cpp
    )
    target_link_libraries(
        cubey_project_planet_tests
        PRIVATE
            cubey::cubey
            cubey_project_warnings
    )
    add_test(NAME planet_frame_tests COMMAND cubey_project_planet_tests)

    add_executable(
        cubey_project_planet_celestial_tests
        planet_atmosphere_adapter.cpp
        planet_celestial.cpp
        planet_celestial_tests.cpp
    )
    target_link_libraries(
        cubey_project_planet_celestial_tests
        PRIVATE
            cubey::cubey
            cubey_project_warnings
    )
    add_test(NAME planet_celestial_tests COMMAND cubey_project_planet_celestial_tests)

    add_executable(
        cubey_project_planet_surface_tests
        planet_camera.cpp
        planet_config.cpp
        planet_frame.cpp
        planet_local_detail.cpp
        planet_local_detail_runtime.cpp
        planet_surface.cpp
        planet_surface_field.cpp
        planet_surface_runtime.cpp
        planet_surface_tests.cpp
    )
    target_link_libraries(
        cubey_project_planet_surface_tests
        PRIVATE
            cubey::cubey
            cubey_project_warnings
    )
    add_test(NAME planet_surface_tests COMMAND cubey_project_planet_surface_tests)

    set(CUBEY_PLANET_HEADLESS_SMOKE_OUTPUT
        "${CMAKE_CURRENT_BINARY_DIR}/planet-headless-smoke.png"
    )
    cubey_add_png_smoke_test(
        planet_headless_writes_png
        cubey_project_planet
        "${CUBEY_PLANET_HEADLESS_SMOKE_OUTPUT}"
        --headless
        --frames 2
    )
    cubey_add_png_stats_test(
        planet_headless_writes_png
        "${CUBEY_PLANET_HEADLESS_SMOKE_OUTPUT}"
        0.00001
        0.00005
    )

    function(cubey_add_planet_visual_smoke name output_file)
        set(output_path "${CMAKE_CURRENT_BINARY_DIR}/${output_file}")
        cubey_add_png_smoke_test(
            "${name}"
            cubey_project_planet
            "${output_path}"
            --headless
            --frames 2
            --planet-pause-time
            ${ARGN}
        )
        cubey_add_png_stats_test(
            "${name}"
            "${output_path}"
            0.00001
            0.00005
        )
    endfunction()

    cubey_add_planet_visual_smoke(
        planet_surface_dawn_writes_png
        planet-surface-dawn.png
        --planet-camera-mode
        surface
        --planet-day-of-year
        80
        --planet-time-hours
        5.5
    )
    cubey_add_planet_visual_smoke(
        planet_surface_day_writes_png
        planet-surface-day.png
        --planet-camera-mode
        surface
        --planet-day-of-year
        80
        --planet-time-hours
        12.0
    )
    cubey_add_planet_visual_smoke(
        planet_surface_night_writes_png
        planet-surface-night.png
        --planet-camera-mode
        surface
        --planet-day-of-year
        80
        --planet-time-hours
        0.0
    )
    cubey_add_planet_visual_smoke(
        planet_surface_sun_dawn_writes_png
        planet-surface-sun-dawn.png
        --planet-camera-mode
        surface
        --planet-day-of-year
        80
        --planet-time-hours
        4.75
        --planet-camera-surface-look
        sun
        --planet-camera-surface-pitch-deg
        22
    )
    cubey_add_planet_visual_smoke(
        planet_surface_antisun_dawn_writes_png
        planet-surface-antisun-dawn.png
        --planet-camera-mode
        surface
        --planet-day-of-year
        80
        --planet-time-hours
        4.75
        --planet-camera-surface-look
        antisun
        --planet-camera-surface-pitch-deg
        22
    )
    cubey_add_planet_visual_smoke(
        planet_orbit_lit_writes_png
        planet-orbit-lit.png
        --planet-camera-mode
        orbit
        --planet-day-of-year
        80
        --planet-time-hours
        12.0
    )
    cubey_add_planet_visual_smoke(
        planet_orbit_terminator_writes_png
        planet-orbit-terminator.png
        --planet-camera-mode
        orbit
        --planet-day-of-year
        80
        --planet-time-hours
        18.0
    )
    cubey_add_planet_visual_smoke(
        planet_day_moon_writes_png
        planet-day-moon.png
        --planet-camera-mode
        surface
        --planet-day-of-year
        87.4
        --planet-time-hours
        12.0
    )
    cubey_add_planet_visual_smoke(
        planet_moon_occlusion_writes_png
        planet-moon-occlusion.png
        --planet-camera-mode
        orbit
        --planet-day-of-year
        88
        --planet-time-hours
        18.13
    )
    set_tests_properties(
        planet_celestial_tests
        planet_headless_writes_png
        planet_headless_writes_png_stats
        planet_surface_dawn_writes_png
        planet_surface_dawn_writes_png_stats
        planet_surface_day_writes_png
        planet_surface_day_writes_png_stats
        planet_surface_night_writes_png
        planet_surface_night_writes_png_stats
        planet_surface_sun_dawn_writes_png
        planet_surface_sun_dawn_writes_png_stats
        planet_surface_antisun_dawn_writes_png
        planet_surface_antisun_dawn_writes_png_stats
        planet_orbit_lit_writes_png
        planet_orbit_lit_writes_png_stats
        planet_orbit_terminator_writes_png
        planet_orbit_terminator_writes_png_stats
        planet_day_moon_writes_png
        planet_day_moon_writes_png_stats
        planet_moon_occlusion_writes_png
        planet_moon_occlusion_writes_png_stats
        PROPERTIES LABELS sky
    )
    cubey_add_planet_visual_smoke(
        planet_wire_lod_writes_png
        planet-wire-lod.png
        --planet-camera-mode
        surface
        --debug-view
        wireframe
    )
    cubey_add_planet_visual_smoke(
        planet_local_detail_wireframe_writes_png
        planet-local-detail-wireframe.png
        --planet-camera-mode
        surface
        --planet-camera-altitude-m
        2000
        --planet-local-detail-lod-levels
        4
        --planet-local-detail-cells
        16
        --debug-view
        local-detail-wireframe
    )
    cubey_add_planet_visual_smoke(
        planet_local_detail_blend_writes_png
        planet-local-detail-blend.png
        --planet-camera-mode
        surface
        --planet-camera-altitude-m
        2000
        --planet-local-detail-lod-levels
        4
        --planet-local-detail-cells
        16
        --debug-view
        local-detail-blend
    )
    cubey_add_planet_visual_smoke(
        planet_local_detail_height_writes_png
        planet-local-detail-height.png
        --planet-camera-mode
        surface
        --planet-camera-altitude-m
        750
        --planet-local-detail-lod-levels
        5
        --planet-local-detail-cells
        32
        --debug-view
        local-detail-height
    )
    cubey_add_planet_visual_smoke(
        planet_local_detail_features_writes_png
        planet-local-detail-features.png
        --planet-camera-mode
        surface
        --planet-camera-altitude-m
        750
        --planet-local-detail-lod-levels
        5
        --planet-local-detail-cells
        32
        --debug-view
        local-detail-features
    )
    cubey_add_planet_visual_smoke(
        planet_local_detail_final_writes_png
        planet-local-detail-final.png
        --planet-camera-mode
        surface
        --planet-camera-altitude-m
        750
        --planet-local-detail-lod-levels
        5
        --planet-local-detail-cells
        32
        --debug-view
        local-detail-final
    )
    cubey_add_planet_visual_smoke(
        planet_surface_field_writes_png
        planet-surface-field.png
        --planet-camera-mode
        orbit
        --debug-view
        moisture
    )
endif()
